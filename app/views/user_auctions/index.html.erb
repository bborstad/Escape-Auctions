<h1>My Auctions</h1>

<% auctions.each do |auction| %>
  <div id="<%= dom_id(auction) %>">
    <br>
    <p>
      <%= auction.title %>
      <%= link_to '🔎', auction_path(auction) %>
      <%= link_to '🖋', auction_quiz_path(auction) %>
      <%= link_to '🗑', auction_path(auction), method: :delete %>
    </p>

    <p>
      <%= truncate auction.description, length: 75, separator: ' ' %>
    </p>
  </div>
<% end %>

<p><%= link_to "Home", home_path %></p>
<p><%= link_to "All Auctions", auctions_path %></p>

